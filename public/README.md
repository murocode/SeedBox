Place your logo image file here so the site can use it in the header.

Filename: seedbox-logo.png
Path (relative to repo root): public/seedbox-logo.png

Instructions:
- Save the provided logo image to `public/seedbox-logo.png`.
- Commit the file to the repository.

If you prefer a different filename, update the `src/components/SiteShell.tsx` and `src/components/SiteShellClient.tsx` image `src` attribute accordingly.

Example (from project root):

```bash
# copy an image into the public folder (PowerShell)
cp C:\path\to\your\logo.png public\seedbox-logo.png
# or using git to add the file
git add public/seedbox-logo.png && git commit -m "Add site logo"
```
